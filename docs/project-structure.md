# 🏗️ Uniserve - Project Structure

> **آخر تحديث:** 2025-12-07

---

## 📋 نظرة عامة

**Uniserve** هو مشروع API Gateway مبني باستخدام:
- **Next.js 16** (App Router)
- **React 19**
- **TypeScript 5**
- **Prisma ORM** (قاعدة بيانات SQLite محلياً)
- **next-intl** (دعم اللغات: العربية والإنجليزية)
- **NextAuth v5** (المصادقة)
- **Tailwind CSS 4**

---

## 📁 هيكل المشروع

```
Uniserve/
├── 📁 src/                      # الكود المصدري الرئيسي
│   ├── 📁 app/                  # App Router (Next.js)
│   │   ├── 📁 [locale]/         # مسارات متعددة اللغات
│   │   │   ├── 📁 (auth)/       # صفحات المصادقة (Route Group)
│   │   │   │   ├── 📁 login/    # تسجيل الدخول
│   │   │   │   └── 📁 register/ # إنشاء حساب جديد
│   │   │   ├── 📁 (dashboard)/  # لوحة التحكم (Route Group)
│   │   │   │   ├── 📁 dashboard/# الصفحة الرئيسية للوحة
│   │   │   │   ├── 📁 docs/     # وثائق API
│   │   │   │   └── layout.tsx   # تخطيط الداشبورد
│   │   │   ├── 📁 (public)/     # الصفحات العامة (Route Group)
│   │   │   │   ├── 📁 about/    # حول المشروع
│   │   │   │   ├── 📁 api-docs/ # وثائق API العامة
│   │   │   │   ├── 📁 developers/ # صفحة المطورين
│   │   │   │   ├── 📁 pricing/  # الأسعار
│   │   │   │   ├── 📁 solutions/# الحلول
│   │   │   │   ├── layout.tsx   # تخطيط الصفحات العامة
│   │   │   │   └── page.tsx     # الصفحة الرئيسية
│   │   │   └── layout.tsx       # التخطيط الجذري مع اللغات
│   │   ├── 📁 api/              # مسارات API
│   │   │   ├── 📁 auth/         # API المصادقة
│   │   │   ├── 📁 gateway/      # API البوابة الرئيسية
│   │   │   ├── 📁 keys/         # إدارة مفاتيح API
│   │   │   └── 📁 register/     # تسجيل المستخدمين
│   │   ├── 📁 lib/              # مكتبات خاصة بـ App
│   │   ├── favicon.ico          # أيقونة الموقع
│   │   └── globals.css          # الأنماط العامة
│   │
│   ├── 📁 components/           # المكونات المشتركة
│   │   ├── DashboardNav.tsx     # شريط التنقل في الداشبورد
│   │   ├── Footer.tsx           # ذيل الصفحة
│   │   ├── KeyManager.tsx       # إدارة مفاتيح API
│   │   ├── LanguageSwitcher.tsx # تبديل اللغات
│   │   └── Navbar.tsx           # شريط التنقل الرئيسي
│   │
│   ├── 📁 i18n/                 # إعدادات الترجمة
│   │   ├── request.ts           # طلبات الترجمة
│   │   └── routing.ts           # توجيه اللغات
│   │
│   ├── 📁 lib/                  # المكتبات المساعدة
│   │   ├── actions.ts           # Server Actions
│   │   ├── auth.ts              # إعدادات NextAuth
│   │   ├── navigation.ts        # التنقل مع اللغات
│   │   └── prisma.ts            # إعداد Prisma Client
│   │
│   └── middleware.ts            # Middleware للغات والمصادقة
│
├── 📁 messages/                 # ملفات الترجمة
│   ├── ar.json                  # الترجمة العربية (~15KB)
│   └── en.json                  # الترجمة الإنجليزية (~12KB)
│
├── 📁 prisma/                   # قاعدة البيانات
│   ├── schema.prisma            # مخطط Prisma
│   └── dev.db                   # قاعدة بيانات SQLite المحلية
│
├── 📁 public/                   # الملفات الثابتة
│   ├── file.svg
│   ├── globe.svg
│   ├── next.svg
│   ├── vercel.svg
│   └── window.svg
│
├── 📁 scripts/                  # سكربتات مساعدة
│   └── seed-test.ts             # بذر بيانات للاختبار
│
├── 📁 docs/                     # التوثيق
│   └── project-structure.md     # هذا الملف
│
├── 📁 node_modules/             # الحزم المثبتة
├── 📁 .next/                    # ملفات البناء (مُولّدة)
├── 📁 .git/                     # Git Repository
│
├── .env                         # متغيرات البيئة
├── .gitignore                   # ملفات Git المتجاهلة
├── package.json                 # تعريف المشروع والحزم
├── pnpm-lock.yaml               # قفل إصدارات pnpm
├── tsconfig.json                # إعدادات TypeScript
├── next.config.ts               # إعدادات Next.js
├── postcss.config.mjs           # إعدادات PostCSS
├── eslint.config.mjs            # إعدادات ESLint
├── prisma.config.ts             # إعدادات Prisma
└── README.md                    # وصف المشروع
```

---

## 🔗 Route Groups

| المجموعة | المسار | الوصف |
|:---------|:-------|:-------|
| `(auth)` | `/[locale]/login`, `/[locale]/register` | صفحات المصادقة |
| `(dashboard)` | `/[locale]/dashboard/*` | لوحة التحكم المحمية |
| `(public)` | `/[locale]/*` | الصفحات العامة |

---

## 🔌 API Routes

| المسار | الوظيفة |
|:-------|:--------|
| `/api/auth/*` | مسارات NextAuth للمصادقة |
| `/api/gateway/*` | البوابة الرئيسية للـ API |
| `/api/keys/*` | إنشاء وإدارة مفاتيح API |
| `/api/register` | تسجيل مستخدم جديد |

---

## 🌐 دعم اللغات

- **العربية (ar):** اللغة الافتراضية
- **الإنجليزية (en):** مدعومة بالكامل
- ملفات الترجمة في مجلد `messages/`

---

## ⚙️ سكربتات التشغيل

```bash
pnpm dev      # تشغيل خادم التطوير
pnpm build    # بناء الإنتاج
pnpm start    # تشغيل الإنتاج
pnpm lint     # فحص الكود
```

---

## 📦 الحزم الرئيسية

| الحزمة | الإصدار | الوظيفة |
|:-------|:--------|:--------|
| next | 16.0.7 | إطار العمل |
| react | 19.2.0 | واجهة المستخدم |
| @prisma/client | 5.22.0 | ORM قاعدة البيانات |
| next-auth | 5.0.0-beta.30 | المصادقة |
| next-intl | 4.5.8 | الترجمة |
| tailwindcss | 4 | التنسيق |
| zod | 3.25.76 | التحقق من البيانات |


تحديث هذا الملف باستمرار 